spring:

  r2dbc:
    url: r2dbc:postgresql://${DATABASE_HOST:localhost:5436}/${DATABASE_NAME:postgres-test}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:secret}
    properties:
      schema: "notification"

  liquibase:
    url: jdbc:postgresql://${DATABASE_HOST:localhost:5436}/${DATABASE_NAME:postgres-test}
    user: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:secret}
    change-log: classpath:db.migration/db.changelog-master.yaml
    liquibase-schema: "notification"
    enabled: true

  rabbitmq:
    host: ${RABBIT_HOST:localhost}
    port: ${RABBIT_PORT:5672}
    username: ${RABBIT_USERNAME:M1msoftR@bb!t}
    password: ${RABBIT_PASSWORD:M1msoftR@bb!t}
    exchange: exchange.direct.lamenu

  application:
    name: notification-service

  mail:
    host: smtp.mail.com
    port: 587
    username: saydaliyevelmurod01@gmail.com
    password: Gmcz6tUCHdF9bzPbKgpf
    properties:
      mail:
        smtp:
          auth: true
        starttls:
          enable: true




springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html


server:
  port: 8082

logging:
  level:
    org.example: info

